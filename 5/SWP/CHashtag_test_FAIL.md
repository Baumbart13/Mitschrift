Aufgabe 1.a
====

```C#
public static processedToView{
    public List<Booking>();
    public getUnprocessed(){ try{
        db.Open();
        DbCommand selectCmd = new DbCommand.Commandtext(),
        selectCmd = "select * from PB where processed = O(false)"
        List<Booking>() bookings = new List<Bookings>() = null;
        while( weiß nicht auswendig, wie Nutzung grade, aber sollang datensätze kommen) booking add {
            booking_id = Convert.toInt32([booking_id]);
            lastname = convert.toString([lastname]);
            firstname = convert.toString([firstname]);
            
            processed = Convert.toBool([processed]);
        }
    }
}catch{
    return booking;
}
public void processedToView{
    publicList<Booking> books = new List<Booking>();
    books = db.getUnprocessed();
```

Aufgabe 2
====

```HTML
<tb>
    Modell @bookings
    <thead><tr>
        <td colspan:10>unbearbeitette Buchungen</td><tr><td=ID</td>
                                                        <td>Vorname</td>
                                                        <td>Nachname</td>
                                                        <td>processed</td>
                                                        <td colspan:2></td></tr></thead>
        <tbody>
        <tr>for(Booking b model){
            <td>@booking_id</td>
            <td>@firstname</td>
            <td>processed</td>
            <td asp-controller:"center Books" asp-action:"delete">lösche</td>
            <td asp-controller:"contrbooks" asp-action:"update">bearbeiten</td>
```

Beginn 3
====

```C#
public bool updateProcessed(booking_id){
    DbCommand updateCmd = new DbCommand.Commandtext
    updateCmd = "update processed true(a ka) from DB where booking_id =" booking_id+1 -> nicht sicher beim update

    {
        DbParameterId = new DbParameter.updatecmd();
        paramid.paramname = "id";
        paramid. ... typ = ... typ(int)
        paramid.Value = booking_id.
    }
```

> _"Man muss auf seinen Programmiertest auswendig lernen. Man kann nicht auf Verständnis lernen"_ - Klassenkollegin
